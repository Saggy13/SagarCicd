package actionspac;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.Wait;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Autodrop {

	public static void main(String[] args) {
		
				
		
	}

	/*	WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.get("https://demo.automationtesting.in/AutoComplete.html");
		
		Actions act= new Actions(driver);
		act.sendKeys(driver.findElement(By.xpath("//input[@id='searchbox']")),"india").build().perform();
		//act.sendKeys(driver.findElement(By.xpath(null))"//input[@id='searchbox']","india").build().perform();
		WebDriverWait wait= new WebDriverWait(driver, Duration.ofSeconds(5));
		wait.until(ExpectedConditions.visibilityOfAllElementsLocatedBy(By.xpath("//li[@class='ui-menu-item']")));
		  
		 driver.findElement(By.xpath("//li[@class='ui-menu-item']/following-sibling::li/a[text()='India']")).click();
		 
		 System.out.println("Done successfully");
		 
		
		
	}}
	
	*/



}
